#!/usr/bin/perl

use strict;

my $var = 1;

# Here we learn to compare and make "logical" decisions
# note that we are now using the same line to make comparissons
if ($var == 1) { print "var equals $var\n"; } 
else { print "var does not equals $var\n"; }

print "";
